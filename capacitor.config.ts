import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nuru',
  appName: 'nuru',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
